listen_addresses = '*'
port = 5432
max_connections = 20

# Log Settings
logging_collector = on
log_directory = 'log'
log_filename = 'postgresql-%Y-%m-%d.log'

log_connections = on
log_disconnections = on
log_statement = 'none'

timezone = 'UTC'